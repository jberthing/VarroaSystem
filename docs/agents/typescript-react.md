# TypeScript and React Conventions

- Prefer strict typing; avoid `any` unless there is no practical alternative.
- Keep component changes aligned with existing patterns and naming.
- Preserve existing CSS structure in touched files unless restyling is requested.
- Avoid introducing new abstractions unless they reduce clear repetition in the current task scope.
